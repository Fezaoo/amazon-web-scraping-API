{
    "routes": [
      { "src": "/api/(.*)", "dest": "server.py" },
      { "handle": "filesystem" }
    ],
    "rewrites": [
      { "source": "/(.*)", "destination": "/api/$1" }
    ],
    "builds": [
      { "src": "server.py", "use": "@now/python" }
    ]
  }
  